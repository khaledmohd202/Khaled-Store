import 'package:flutter/material.dart';

class Const {
  static double mediaQueryHeight(context) => MediaQuery.of(context).size.height;
  static double mediaQueryWidth(context) => MediaQuery.of(context).size.width;
}
